import "./style.css";

import { HorizontalConstraint } from "acs";

const horizontalConstraint = HorizontalConstraint.new(42);
console.log("HorizontalConstraint created with line_id:", horizontalConstraint);
